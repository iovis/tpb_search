require "spec_helper"

RSpec.describe TpbSearch do
  it "has a version number" do
    expect(TpbSearch::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
